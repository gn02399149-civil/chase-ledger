rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每個人的資料都放在 users/{uid} 底下，只有登入身分等於 uid 的人可以讀寫
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
